extends base
include mixins/video

block content
    div.container
        div.home__warp
            div.home_menu
                ul 
                    li
                        a(href="#")
                            i.fas.fa-home
                            div 홈
                    li
                        a(href="#")
                            i.fas.fa-compass
                            div 탐색
                    li
                        a(href="#")
                            i.fas.fa-file-video
                            div 구독
                    li
                        a(href="#")
                            i.fas.fa-archive
                            div 보관함

            div.home__content
                each video in videos 
                    +video(video)
                else 
                    span.empty__message 업로드된 비디오가 없습니다.
